import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.learning.tinderswipeapp',
  appName: 'tinder-swipe-app',
  webDir: 'build'
};

export default config;
